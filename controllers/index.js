const UserController = require('./users');
const PlatoonController = require('./platoons');
const CompanyController = require('./companies');
module.exports = {
    UserController,
    PlatoonController,
    CompanyController,
}